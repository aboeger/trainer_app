<%= form_for(@exercise_group) do |f| %>
  <% if @exercise_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise_group.errors.count, "error") %> prohibited this exercise_group from being saved:</h2>

      <ul>
      <% @exercise_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
