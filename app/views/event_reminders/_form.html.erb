<%= form_for(@event_reminder) do |f| %>
  <% if @event_reminder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_reminder.errors.count, "error") %> prohibited this event_reminder from being saved:</h2>

      <ul>
      <% @event_reminder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :start %><br>
    <%= f.datetime_select :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.datetime_select :end %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
